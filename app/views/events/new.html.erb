<div class="container">
  <h1>Let's create a new event!</h1>
  <p>The fields in beige are required, the others are optional!</p>
    <br>
  <%= render 'shared/errors', obj: @event %>

  <%= render 'create-event'%>

</div>
